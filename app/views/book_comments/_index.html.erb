<% book.book_comments.each do |book_comment|%>
  <%= book_comment.user.attachment_image_tag(user, :profile_image, :fill, 50, 50, fallback: "no-image-icon.jpg") %>
  <%= book_comment.user.name %>
  <%= book_comment.comment %>
<% end %>